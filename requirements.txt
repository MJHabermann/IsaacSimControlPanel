# Flask + ROS2 dependencies
flask>=2.3.0
flask-socketio>=5.3.0
flask-cors>=4.0.0
python-socketio>=5.8.0
eventlet>=0.33.0

# ROS2 Python client (installed via ROS2 distribution)
# rclpy is installed with ROS2, not pip

# Optional: for development
python-dotenv>=1.0.0
